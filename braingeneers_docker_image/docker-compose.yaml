# This is the docker-compose configuration that launches the full
# braingeneers stack. Use the Makefile to run it. Example:
#   make start-services
#   make stop-services
version: "3"

services:
  braingeneers-mission-control:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8888:8888
    environment:
      - ENDPOINT=https://s3-west.nrp-nautilus.io
